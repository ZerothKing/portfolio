---
const publications = [
  {
    title: "An Introduction to Disjoint-set Data Structures",
    platform: "IEEE NITK Blog",
    description: "A practical guide to the Disjoint-set Union (DSU) data structure, exploring path compression and union by rank optimizations.",
    link: "https://ieee-nitk.github.io/blog/An-Introduction-to-Disjoint-set-data-structures/",
    date: "Jan 2022",
    icon: "📚"
  },
  {
    title: "Sanjkeet's Substack",
    platform: "Substack",
    description: "Personal newsletter where I share thoughts on software engineering, distributed systems, and technical leadership.",
    link: "https://sanjkeet.substack.com/",
    date: "Ongoing",
    icon: "✍️"
  }
];
---

<section id="writing" class="container section-padding">
  <div class="writing-header">
    <h2 class="title">Writing</h2>
    <p class="writing-subtitle">Thoughts on engineering, systems design, and beyond</p>
  </div>
  <div class="writing-grid">
    {publications.map((pub, index) => (
      <a href={pub.link} target="_blank" rel="noopener" class="writing-card" style={`animation-delay: ${index * 0.1}s`}>
        <div class="card-content">
          <div class="card-top">
            <span class="platform-icon">{pub.icon}</span>
            <div class="meta">
              <span class="platform">{pub.platform}</span>
              <span class="date">{pub.date}</span>
            </div>
          </div>
          
          <h3 class="article-title">{pub.title}</h3>
          <p class="article-description">{pub.description}</p>
          
          <span class="read-link">
            Read article
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
              <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </span>
        </div>
        
        <div class="card-accent" aria-hidden="true"></div>
      </a>
    ))}
  </div>
</section>

<style>
  .writing-header {
    margin-bottom: var(--space-8);
  }
  
  .writing-subtitle {
    color: var(--text-secondary);
    font-size: var(--text-lg);
    margin-top: calc(var(--space-4) * -1);
    margin-bottom: 0;
  }
  
  .writing-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
    gap: var(--space-6);
  }
  
  .writing-card {
    position: relative;
    background: var(--bg-card);
    backdrop-filter: blur(16px);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-lg);
    padding: var(--space-6);
    text-decoration: none;
    overflow: hidden;
    opacity: 0;
    animation: fadeInUp 0.6s var(--ease-out-expo) forwards;
    transition: all var(--duration-normal) var(--ease-out-expo);
  }
  
  .writing-card:hover {
    border-color: var(--accent-color);
    transform: translateY(-6px);
    box-shadow: 0 8px 30px var(--accent-glow);
  }
  
  .writing-card:hover .card-accent {
    opacity: 1;
  }
  
  .writing-card:hover .read-link svg {
    transform: translateX(4px);
  }
  
  .card-content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    height: 100%;
  }
  
  /* Accent glow */
  .card-accent {
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: linear-gradient(180deg, var(--accent-color), var(--primary-color));
    opacity: 0;
    transition: opacity var(--duration-normal) ease;
  }
  
  /* Header */
  .card-top {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-4);
  }
  
  .platform-icon {
    font-size: var(--text-2xl);
    line-height: 1;
  }
  
  .meta {
    display: flex;
    flex-direction: column;
    gap: var(--space-1);
  }
  
  .platform {
    font-weight: 600;
    font-size: var(--text-sm);
    color: var(--accent-color);
  }
  
  .date {
    font-size: var(--text-xs);
    color: var(--text-muted);
  }
  
  /* Content */
  .article-title {
    font-size: var(--text-xl);
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--space-3);
    line-height: 1.3;
    transition: color var(--duration-fast) ease;
  }
  
  .writing-card:hover .article-title {
    color: var(--accent-300);
  }
  
  .article-description {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: var(--space-5);
    flex: 1;
  }
  
  /* Read link */
  .read-link {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-weight: 600;
    font-size: var(--text-sm);
    color: var(--accent-color);
    margin-top: auto;
  }
  
  .read-link svg {
    transition: transform var(--duration-fast) ease;
  }
  
  @media (max-width: 480px) {
    .writing-grid {
      grid-template-columns: 1fr;
    }
    
    .writing-subtitle {
      font-size: var(--text-base);
    }
  }
</style>
